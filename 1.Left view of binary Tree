def rec(root,level,res):
        if not root:
            return
        
        if len(res) == level:
            res.append(root.data)
        rec(root.left,level+1,res)
        rec(root.right,level+1,res)
        
def LeftView(root):
    res = []
    rec(root,0,res)
    return res
